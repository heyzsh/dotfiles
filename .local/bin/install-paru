#!/bin/sh

! [ -x $(command -v git) ] && echo "git not instlled" && exit 1

cwd=$(pwd)
cd $HOME
mkdir __tmp
cd __tmp
git clone https://aur.archlinux.org/paru.git
cd paru
echo "Building paru"
sudo makepkg --noconfirm --skippgpcheck -r -si

cd "$cwd" 
rm -rf $HOME/__tmp

echo "Done"
