#!/bin/sh

current=$(torify curl -s ifconfig.me)
echo "$current"
while true; do
  new=$(torify curl -s ifconfig.me)
  if ! [ "$new" = "$current" ]; then
    current="$new"
    echo "$current"
  fi
  sleep 5
done
