#!/bin/sh

PKGS="$HOME/.local/share/packages"

sudo pacman -Syy

while IFS= read -r p; do
  [ "$(echo "$p" | grep -ic "#")" -gt 0 ] && continue
  paru --needed --noconfirm --removemake -S "$p"
done < "$PKGS"

sudo pacman --noconfirm -Scc
sudo pacman --noconfirm -Rsn $(pacman -Qdtq)
